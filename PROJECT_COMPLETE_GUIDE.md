# 📘 راهنمای کامل پروژه Cold Cake

## 📖 فهرست مطالب

1. [معرفی پروژه](#معرفی-پروژه)
2. [معماری و تکنولوژی‌ها](#معماری-و-تکنولوژی‌ها)
3. [ساختار دیتابیس](#ساختار-دیتابیس)
4. [نقش‌های کاربری و سناریوها](#نقش‌های-کاربری-و-سناریوها)
5. [راهنمای تست قدم‌به‌قدم](#راهنمای-تست-قدم‌به‌قدم)
6. [Troubleshooting](#troubleshooting)

---

## معرفی پروژه

### Cold Cake چیست؟

**Cold Cake** یک سیستم مدیریت کسب‌وکار (Business Management System) جامع است که برای مدیریت فروشگاه‌های طلا و جواهر طراحی شده است. این سیستم ترکیبی از:

- **CRM (Customer Relationship Management)**: مدیریت ارتباط با مشتریان، علاقه‌مندی‌ها، تاریخچه خرید
- **Inventory Management**: مدیریت موجودی و محصولات
- **Sales Management**: مدیریت فروش و تراکنش‌ها
- **Financial Management**: مدیریت مالی شامل بدهی‌ها، حساب‌های بانکی، تراکنش‌ها
- **Marketing Automation**: اتوماسیون بازاریابی و ارسال پیام‌های خودکار
- **Business Intelligence**: تحلیل داده‌ها و پیش‌بینی فروش فصلی
- **Content Management**: مدیریت محتوای بازاریابی با AI enhancement
- **Maps Integration**: استخراج اطلاعات کسب‌وکار از Google Maps
- **Workflow Automation**: یکپارچه‌سازی با n8n برای automation

### ویژگی‌های کلیدی

✅ **احراز هویت با OTP** - ورود با شماره موبایل و کد تأیید SMS  
✅ **مدیریت طلایی** - قیمت‌گذاری محصولات بر اساس قیمت طلا  
✅ **CRM پیشرفته** - تحلیل علاقه‌مندی‌های مشتریان  
✅ **پیش‌بینی فصلی** - پیش‌بینی فروش بر اساس الگوهای فصلی  
✅ **پیام‌رسانی خودکار** - ارسال پیام‌های شخصی‌سازی شده  
✅ **گزارش‌های کامل** - گزارش‌های مالی و خروجی Excel/PDF  
✅ **یکپارچه‌سازی** - اتصال به Google Apps Script و n8n  

---

## معماری و تکنولوژی‌ها

### Frontend Stack
- **Next.js 16.0.10** - Framework اصلی با App Router
- **React 19.2.1** - کتابخانه UI
- **TypeScript 5** - Type safety
- **Tailwind CSS v4** - Styling
- **Recharts** - نمودارها و charts
- **Heroicons** - آیکون‌ها

### Backend Stack
- **Next.js API Routes** - Server Components & API endpoints
- **Prisma ORM 6.4.0** - Database ORM
- **PostgreSQL** - Database
- **JWT** - Authentication
- **bcryptjs** - Hashing OTP codes

### Services & Integrations
- **Tabansms API** - ارسال پیامک OTP (خط خدماتی 3000505)
- **n8n** - Workflow automation (https://autocoldcake.runflare.run)
- **Google Apps Script** - مدیریت فایل‌های Google Drive
- **Gemini API** - AI برای enhancement محتوا
- **OpenAI API** - AI برای تحلیل و پیش‌بینی

### Deployment
- **Runflare** - Platform deployment
- **Docker** - Containerization
- **Production Build** - `npm run build && npm start`

---

## ساختار دیتابیس

پروژه شامل **30 مدل Prisma** است که در 11 دسته تقسیم شده‌اند:

### 1. Authentication (3 مدل)
- `User` - کاربران سیستم
- `SmsCode` - کدهای OTP
- `Session` - جلسات کاربران

### 2. Customer Management (1 مدل)
- `Customer` - اطلاعات مشتریان

### 3. Product Management (2 مدل)
- `Product` - محصولات
- `Inventory` - موجودی انبار

### 4. Sales & Financial (4 مدل)
- `Sale` - فروش‌ها
- `SaleItem` - آیتم‌های هر فروش
- `Debt` - بدهی‌ها
- `Purchase` - خریدها

### 5. Banking (2 مدل)
- `BankAccount` - حساب‌های بانکی
- `BankTransaction` - تراکنش‌های بانکی

### 6. Messaging (2 مدل)
- `Message` - پیام‌های ارسال شده
- `MessageTemplate` - قالب‌های پیام

### 7. Analytics (4 مدل)
- `CustomerInterest` - علاقه‌مندی‌های مشتریان
- `SeasonalPrediction` - پیش‌بینی‌های فصلی
- `Visit` - بازدیدها
- `SocialInteraction` - تعاملات اجتماعی

### 8. Gold Price (2 مدل)
- `GoldPrice` - قیمت فعلی طلا
- `GoldPriceHistory` - تاریخچه قیمت

### 9. Pricing (3 مدل)
- `PriceChange` - تغییرات قیمت
- `PriceUpdateLog` - لاگ به‌روزرسانی
- `ProductPriceUpdate` - جزئیات تغییرات

### 10. Content & Automation (2 مدل)
- `Content` - محتوای بازاریابی
- `AutomatedMessageRun` - اجراهای پیام خودکار

### 11. Maps Scraper (3 مدل)
- `MapsScrapeTarget` - اهداف اسکراپ
- `ScrapedBusiness` - کسب‌وکارهای استخراج شده
- `MapsScrapeRun` - اجراهای اسکراپ

### 12. Settings (2 مدل)
- `BusinessSettings` - تنظیمات کسب‌وکار
- `IntegrationSettings` - تنظیمات یکپارچه‌سازی

---

## نقش‌های کاربری و سناریوها

### نقش اصلی: مدیر فروشگاه (Business Owner)

مدیر فروشگاه می‌تواند:

1. **مدیریت مشتریان**
   - افزودن/ویرایش/حذف مشتری
   - مشاهده تاریخچه خرید
   - مشاهده علاقه‌مندی‌ها
   - مدیریت بدهی‌ها

2. **مدیریت محصولات**
   - افزودن/ویرایش/حذف محصول
   - مدیریت موجودی
   - تنظیم قیمت (دستی یا خودکار بر اساس طلا)

3. **مدیریت فروش**
   - ثبت فروش جدید
   - مشاهده گزارش‌های فروش
   - تحلیل فروش ماهانه/سالانه

4. **مدیریت مالی**
   - ثبت بدهی‌های مشتریان
   - مدیریت حساب‌های بانکی
   - مشاهده گزارش‌های مالی
   - Export گزارش‌ها

5. **بازاریابی**
   - ارسال پیام به مشتریان
   - ایجاد محتوای بازاریابی
   - استفاده از AI برای enhancement
   - تنظیم پیام‌های خودکار

6. **تحلیل و پیش‌بینی**
   - مشاهده علاقه‌مندی‌های مشتریان
   - مشاهده پیش‌بینی‌های فصلی
   - تصمیم‌گیری بر اساس تحلیل‌ها

---

## راهنمای تست قدم‌به‌قدم

این راهنما شامل **50 قدم تست** است که به شما کمک می‌کند تمام بخش‌های سیستم را بررسی کنید.

---

### بخش 1: راه‌اندازی اولیه (5 قدم)

#### قدم 1: بررسی Environment Variables
- [ ] فایل `.env.local` را بررسی کنید
- [ ] مطمئن شوید `DATABASE_URL` تنظیم شده است
- [ ] مطمئن شوید `JWT_SECRET` تنظیم شده است
- [ ] مطمئن شوید `TABANSMS_API_KEY` تنظیم شده است
- [ ] بررسی کنید سایر متغیرهای اختیاری (n8n, Gemini, OpenAI) تنظیم شده‌اند

**نتیجه مورد انتظار:** همه متغیرهای ضروری باید تنظیم باشند

#### قدم 2: اجرای Migration دیتابیس
- [ ] دستور `npx prisma migrate dev` را اجرا کنید
- [ ] بررسی کنید که migration موفق بوده است
- [ ] بررسی کنید که جداول در دیتابیس ایجاد شده‌اند

**نتیجه مورد انتظار:** 30 جدول باید در دیتابیس ایجاد شوند

#### قدم 3: Generate Prisma Client
- [ ] دستور `npx prisma generate` را اجرا کنید
- [ ] بررسی کنید که Prisma Client در `node_modules/.prisma/client` ایجاد شده است

**نتیجه مورد انتظار:** Prisma Client باید generate شود بدون خطا

#### قدم 4: Build پروژه
- [ ] دستور `npm run build` را اجرا کنید
- [ ] بررسی کنید که build موفق بوده است
- [ ] بررسی کنید که هیچ خطای TypeScript وجود ندارد

**نتیجه مورد انتظار:** Build باید با موفقیت کامل شود

#### قدم 5: Start پروژه
- [ ] دستور `npm start` را اجرا کنید (یا `npm run dev` برای development)
- [ ] بررسی کنید که سرور روی port 3000 (یا port مشخص شده) اجرا می‌شود
- [ ] بررسی کنید که لاگ‌های `[DEPLOY]` نمایش داده می‌شوند

**نتیجه مورد انتظار:** سرور باید به درستی start شود و آماده دریافت request باشد

---

### بخش 2: Authentication (3 قدم)

#### قدم 6: تست صفحه ورود
- [ ] بروید به `https://coldcake.ir/auth/login`
- [ ] بررسی کنید که صفحه به درستی لود می‌شود
- [ ] بررسی کنید که فرم ورود شماره موبایل نمایش داده می‌شود

**نتیجه مورد انتظار:** صفحه ورود باید بدون خطا نمایش داده شود

#### قدم 7: تست دریافت کد OTP
- [ ] شماره موبایل معتبر خود را وارد کنید (مثلاً 09121234567)
- [ ] روی دکمه "ارسال کد تأیید" کلیک کنید
- [ ] بررسی کنید که پیام "کد تأیید ارسال شد" نمایش داده می‌شود
- [ ] بررسی کنید که پیامک حاوی کد 4 رقمی دریافت می‌کنید

**نتیجه مورد انتظار:** باید پیامک OTP دریافت کنید

#### قدم 8: تست ورود با کد OTP
- [ ] کد 4 رقمی دریافت شده را وارد کنید
- [ ] روی دکمه "ورود به داشبورد" کلیک کنید
- [ ] بررسی کنید که به صفحه `/dashboard` redirect می‌شوید
- [ ] بررسی کنید که در مرورگر cookie `auth_token` تنظیم شده است

**نتیجه مورد انتظار:** باید با موفقیت وارد سیستم شوید و به داشبورد بروید

---

### بخش 3: ورود داده‌های اولیه (10 قدم)

#### قدم 9: ایجاد مشتری اول (از طریق API)
- [ ] در Console مرورگر (F12) دستور زیر را اجرا کنید:
```javascript
fetch('/api/customers', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    firstName: 'علی',
    lastName: 'احمدی',
    phone: '09121234567',
    email: 'ali@example.com'
  })
}).then(r => r.json()).then(console.log);
```
- [ ] بررسی کنید که مشتری با موفقیت ایجاد شده است
- [ ] ID مشتری را یادداشت کنید

**نتیجه مورد انتظار:** باید پاسخ JSON با اطلاعات مشتری ایجاد شده برگردد

#### قدم 10: ایجاد چند مشتری دیگر
- [ ] 4-5 مشتری دیگر با اطلاعات مختلف ایجاد کنید
- [ ] از نام‌های مختلف استفاده کنید
- [ ] از شماره تلفن‌های مختلف استفاده کنید

**نتیجه مورد انتظار:** باید 5-6 مشتری در دیتابیس داشته باشید

#### قدم 11: بررسی لیست مشتریان در UI
- [ ] بروید به `/messages`
- [ ] تب "لیست مشتریان" را انتخاب کنید
- [ ] بررسی کنید که همه مشتریان ایجاد شده نمایش داده می‌شوند

**نتیجه مورد انتظار:** باید لیست مشتریان در UI نمایش داده شود

#### قدم 12: ایجاد محصول اول (از طریق API)
```javascript
fetch('/api/products', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'انگشتر طلای 18 عیار',
    color: 'زرد',
    originalPrice: 5000000,
    finalPrice: 5000000,
    category: 'انگشتر',
    stock: 5,
    description: 'انگشتر طلای 18 عیار با نگین'
  })
}).then(r => r.json()).then(console.log);
```
- [ ] بررسی کنید که محصول ایجاد شده است

**نتیجه مورد انتظار:** باید محصول با موفقیت ایجاد شود

#### قدم 13: ایجاد چند محصول دیگر
- [ ] حداقل 10 محصول دیگر با دسته‌بندی‌های مختلف ایجاد کنید:
  - انگشتر (3-4 محصول)
  - گردنبند (2-3 محصول)
  - دستبند (2-3 محصول)
  - دیگر (1-2 محصول)

**نتیجه مورد انتظار:** باید حداقل 11 محصول در دیتابیس داشته باشید

#### قدم 14: ثبت فروش اول
```javascript
// ابتدا ID مشتری و محصول را از قدم‌های قبل بگیرید
fetch('/api/sales', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    customerId: 'CUSTOMER_ID_HERE',
    customerName: 'علی احمدی',
    amount: 5000000,
    date: new Date().toISOString(),
    month: new Date().getMonth() + 1,
    year: new Date().getFullYear(),
    items: [{
      productId: 'PRODUCT_ID_HERE',
      productName: 'انگشتر طلای 18 عیار',
      quantity: 1,
      unitPrice: 5000000
    }]
  })
}).then(r => r.json()).then(console.log);
```
- [ ] بررسی کنید که فروش ثبت شده است

**نتیجه مورد انتظار:** باید فروش با موفقیت ثبت شود

#### قدم 15: ثبت چند فروش دیگر
- [ ] 4-5 فروش دیگر برای مشتریان مختلف ثبت کنید
- [ ] از محصولات مختلف استفاده کنید
- [ ] از تاریخ‌های مختلف استفاده کنید

**نتیجه مورد انتظار:** باید حداقل 6 فروش در دیتابیس داشته باشید

#### قدم 16: ثبت بدهی اول
```javascript
fetch('/api/debts', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    customerId: 'CUSTOMER_ID_HERE',
    customerName: 'علی احمدی',
    amount: 2000000,
    dueDate: new Date(Date.now() + 30*24*60*60*1000).toISOString(), // 30 روز بعد
    status: 'pending',
    type: 'received'
  })
}).then(r => r.json()).then(console.log);
```
- [ ] بررسی کنید که بدهی ثبت شده است

**نتیجه مورد انتظار:** باید بدهی با موفقیت ثبت شود

#### قدم 17: ثبت چند بدهی دیگر
- [ ] 2-3 بدهی دیگر ثبت کنید
- [ ] برخی را با status 'pending' و برخی را 'paid' ثبت کنید

**نتیجه مورد انتظار:** باید حداقل 4 بدهی در دیتابیس داشته باشید

#### قدم 18: تنظیم قیمت طلا
- [ ] بروید به `/gold-price`
- [ ] قیمت فعلی طلا را وارد کنید (مثلاً 50000000 تومان)
- [ ] روی دکمه به‌روزرسانی کلیک کنید
- [ ] بررسی کنید که قیمت ذخیره شده است

**نتیجه مورد انتظار:** قیمت طلا باید به‌روزرسانی شود

---

### بخش 4: تست Features اصلی (15 قدم)

#### قدم 19: بررسی Dashboard
- [ ] بروید به `/dashboard`
- [ ] بررسی کنید که کارت‌های آمار نمایش داده می‌شوند:
  - [ ] فروش کل
  - [ ] بدهی‌ها
  - [ ] موجودی
  - [ ] سرمایه در گردش
- [ ] بررسی کنید که قیمت طلا نمایش داده می‌شود
- [ ] بررسی کنید که نمودارها نمایش داده می‌شوند (اگر داده وجود دارد)

**نتیجه مورد انتظار:** Dashboard باید تمام آمار را نمایش دهد

#### قدم 20: تست جستجوی مشتری
- [ ] بروید به `/messages`
- [ ] تب "لیست مشتریان"
- [ ] در فیلد جستجو نام یک مشتری را وارد کنید
- [ ] بررسی کنید که فقط مشتریان مرتبط نمایش داده می‌شوند

**نتیجه مورد انتظار:** جستجو باید فیلتر صحیح انجام دهد

#### قدم 21: تست ارسال پیام
- [ ] بروید به `/messages`
- [ ] تب "ارسال پیام"
- [ ] یک مشتری را از dropdown انتخاب کنید
- [ ] متن پیامی را وارد کنید (مثلاً "سلام، محصول جدیدی داریم")
- [ ] پلتفرم را انتخاب کنید (Telegram)
- [ ] روی دکمه ارسال کلیک کنید
- [ ] بررسی کنید که پیام "پیام با موفقیت ارسال شد" نمایش داده می‌شود

**نتیجه مورد انتظار:** پیام باید ثبت شود (ارسال واقعی نیاز به تنظیمات اضافی دارد)

#### قدم 22: بررسی تاریخچه پیام‌ها
- [ ] تب "تاریخچه" را انتخاب کنید
- [ ] بررسی کنید که پیام ارسال شده در لیست نمایش داده می‌شود
- [ ] بررسی کنید که اطلاعات صحیح نمایش داده می‌شود (مشتری، پلتفرم، تاریخ)

**نتیجه مورد انتظار:** باید تاریخچه پیام‌ها نمایش داده شود

#### قدم 23: بررسی Reports - Summary
- [ ] بروید به `/reports`
- [ ] تب "Summary" را انتخاب کنید
- [ ] بررسی کنید که آمار کلی نمایش داده می‌شود:
  - [ ] فروش کل ماه
  - [ ] بدهی‌ها
  - [ ] موجودی
  - [ ] سرمایه در گردش
- [ ] بررسی کنید که نمودار فروش ماهانه نمایش داده می‌شود

**نتیجه مورد انتظار:** گزارش Summary باید کامل نمایش داده شود

#### قدم 24: بررسی Reports - Sales
- [ ] تب "Sales" را انتخاب کنید
- [ ] بررسی کنید که لیست فروش‌ها نمایش داده می‌شود
- [ ] بررسی کنید که می‌توانید ماه و سال را تغییر دهید
- [ ] بررسی کنید که فیلتر کار می‌کند

**نتیجه مورد انتظار:** لیست فروش‌ها باید نمایش داده شود

#### قدم 25: بررسی Reports - Debts
- [ ] تب "Debts" را انتخاب کنید
- [ ] بررسی کنید که لیست بدهی‌ها نمایش داده می‌شود
- [ ] بررسی کنید که بدهی‌های pending و paid نمایش داده می‌شوند

**نتیجه مورد انتظار:** لیست بدهی‌ها باید نمایش داده شود

#### قدم 26: بررسی Reports - Inventory
- [ ] تب "Inventory" را انتخاب کنید
- [ ] بررسی کنید که لیست محصولات با موجودی نمایش داده می‌شود
- [ ] بررسی کنید که نمودار موجودی نمایش داده می‌شود

**نتیجه مورد انتظار:** موجودی محصولات باید نمایش داده شود

#### قدم 27: تست Export Excel - Summary
- [ ] در تب Summary
- [ ] روی دکمه "Export Excel" کلیک کنید
- [ ] بررسی کنید که فایل Excel دانلود می‌شود
- [ ] فایل را باز کنید و بررسی کنید که داده‌ها صحیح هستند

**نتیجه مورد انتظار:** باید فایل Excel با داده‌های صحیح دانلود شود

#### قدم 28: تست Export PDF - Summary
- [ ] روی دکمه "Export PDF" کلیک کنید
- [ ] بررسی کنید که فایل PDF دانلود می‌شود
- [ ] فایل را باز کنید و بررسی کنید

**نتیجه مورد انتظار:** باید فایل PDF با داده‌های صحیح دانلود شود

#### قدم 29: بررسی Analytics - Interests
- [ ] بروید به `/analytics`
- [ ] تب "Interests" را انتخاب کنید
- [ ] بررسی کنید که علاقه‌مندی‌های مشتریان نمایش داده می‌شود
- [ ] بررسی کنید که اطلاعات صحیح است (productType, colors, priceRange)

**نتیجه مورد انتظار:** علاقه‌مندی‌های مشتریان باید نمایش داده شود

#### قدم 30: بررسی Analytics - Predictions
- [ ] تب "Predictions" را انتخاب کنید
- [ ] بررسی کنید که پیش‌بینی‌های فصلی نمایش داده می‌شوند (اگر وجود دارند)
- [ ] اگر پیش‌بینی وجود ندارد، روی دکمه "Generate Predictions" کلیک کنید
- [ ] صبر کنید تا پیش‌بینی‌ها ایجاد شوند

**نتیجه مورد انتظار:** پیش‌بینی‌های فصلی باید نمایش داده شوند

#### قدم 31: بررسی Gold Price Management
- [ ] بروید به `/gold-price`
- [ ] بررسی کنید که قیمت فعلی طلا نمایش داده می‌شود
- [ ] بررسی کنید که نمودار تاریخچه قیمت نمایش داده می‌شود
- [ ] قیمت جدیدی وارد کنید و به‌روزرسانی کنید
- [ ] بررسی کنید که نمودار به‌روز می‌شود

**نتیجه مورد انتظار:** مدیریت قیمت طلا باید به درستی کار کند

#### قدم 32: بررسی Settings - Business
- [ ] بروید به `/settings`
- [ ] تب "تنظیمات کسب‌وکار" را انتخاب کنید
- [ ] اطلاعات کسب‌وکار را وارد کنید:
  - [ ] نام کسب‌وکار
  - [ ] شماره تماس
  - [ ] کانال تلگرام
  - [ ] کانال روبیکا
  - [ ] شماره واتساپ
  - [ ] صفحه اینستاگرام
  - [ ] آدرس
- [ ] روی دکمه ذخیره کلیک کنید
- [ ] بررسی کنید که تنظیمات ذخیره شده است

**نتیجه مورد انتظار:** تنظیمات باید ذخیره شوند

#### قدم 33: بررسی Settings - Integrations
- [ ] تب "یکپارچه‌سازی‌ها" را انتخاب کنید
- [ ] بررسی کنید که فرم نمایش داده می‌شود
- [ ] Google Cloud Client ID و Client Secret را وارد کنید (اگر دارید)
- [ ] n8n Webhook URL را وارد کنید
- [ ] Gemini API Key را وارد کنید (اگر دارید)
- [ ] OpenAI API Key را وارد کنید (اگر دارید)
- [ ] روی دکمه ذخیره کلیک کنید

**نتیجه مورد انتظار:** تنظیمات یکپارچه‌سازی باید ذخیره شوند

---

### بخش 5: تست Features پیشرفته (10 قدم)

#### قدم 34: بررسی Content Management
- [ ] بروید به `/content`
- [ ] بررسی کنید که لیست محتوا نمایش داده می‌شود (اگر وجود دارد)
- [ ] روی دکمه "افزودن محتوا" کلیک کنید
- [ ] فرم را پر کنید:
  - [ ] نوع محتوا (image/video/audio)
  - [ ] فایل را انتخاب کنید
  - [ ] کپشن را وارد کنید
- [ ] بررسی کنید که می‌توانید محتوا را آپلود کنید

**نتیجه مورد انتظار:** باید بتوانید محتوا آپلود کنید

#### قدم 35: تست AI Enhancement برای محتوا
- [ ] یک محتوا را انتخاب کنید
- [ ] روی دکمه "AI Enhancement" کلیک کنید (اگر وجود دارد)
- [ ] صبر کنید تا AI کپشن را بهبود دهد
- [ ] بررسی کنید که کپشن بهبود یافته نمایش داده می‌شود

**نتیجه مورد انتظار:** AI باید کپشن را بهبود دهد (نیاز به API key دارد)

#### قدم 36: بررسی Maps Scraper
- [ ] بروید به `/maps-scraper`
- [ ] بررسی کنید که لیست اهداف اسکراپ نمایش داده می‌شود
- [ ] روی دکمه "افزودن هدف جدید" کلیک کنید
- [ ] فرم را پر کنید:
  - [ ] Keyword (مثلاً "طلا فروشی")
  - [ ] City (مثلاً "تهران")
  - [ ] Radius (مثلاً 5000 متر)
- [ ] هدف را ذخیره کنید

**نتیجه مورد انتظار:** باید بتوانید هدف اسکراپ ایجاد کنید

#### قدم 37: تست اجرای Maps Scraper
- [ ] یک هدف اسکراپ را انتخاب کنید
- [ ] روی دکمه "اجرای اسکراپ" کلیک کنید
- [ ] بررسی کنید که پیشرفت نمایش داده می‌شود
- [ ] صبر کنید تا اسکراپ کامل شود

**نتیجه مورد انتظار:** اسکراپ باید اجرا شود و کسب‌وکارها استخراج شوند (نیاز به تنظیمات Google Maps API دارد)

#### قدم 38: بررسی Maps Scraper Results
- [ ] بعد از اتمام اسکراپ
- [ ] لیست کسب‌وکارهای استخراج شده را بررسی کنید
- [ ] بررسی کنید که اطلاعات صحیح است (نام، آدرس، تلفن)

**نتیجه مورد انتظار:** باید لیست کسب‌وکارها نمایش داده شود

#### قدم 39: بررسی Workflows
- [ ] بروید به `/workflows`
- [ ] بررسی کنید که لیست workflow ها نمایش داده می‌شود
- [ ] بررسی کنید که workflow های فعال و غیرفعال نمایش داده می‌شوند

**نتیجه مورد انتظار:** باید لیست workflow ها نمایش داده شود

#### قدم 40: تست Toggle Workflow
- [ ] یک workflow را انتخاب کنید
- [ ] روی دکمه toggle کلیک کنید تا فعال/غیرفعال شود
- [ ] بررسی کنید که وضعیت تغییر کرده است

**نتیجه مورد انتظار:** باید بتوانید workflow را فعال/غیرفعال کنید

#### قدم 41: تست اجرای دستی Workflow
- [ ] یک workflow فعال را انتخاب کنید
- [ ] روی دکمه "اجرای دستی" کلیک کنید
- [ ] بررسی کنید که workflow اجرا می‌شود
- [ ] بررسی کنید که نتیجه نمایش داده می‌شود

**نتیجه مورد انتظار:** باید بتوانید workflow را دستی اجرا کنید

#### قدم 42: بررسی Automated Messaging
- [ ] بروید به `/messages`
- [ ] تب "خودکار" را انتخاب کنید
- [ ] بررسی کنید که تنظیمات پیام خودکار نمایش داده می‌شود
- [ ] بررسی کنید که می‌توانید شرایط ارسال را تنظیم کنید

**نتیجه مورد انتظار:** باید بتوانید پیام‌های خودکار را تنظیم کنید

#### قدم 43: تست اجرای Automated Messaging
- [ ] روی دکمه "اجرای پیام‌های خودکار" کلیک کنید
- [ ] بررسی کنید که پیام‌ها ارسال می‌شوند
- [ ] بررسی کنید که لاگ ارسال نمایش داده می‌شود

**نتیجه مورد انتظار:** باید بتوانید پیام‌های خودکار را اجرا کنید

---

### بخش 6: تست Integration (5 قدم)

#### قدم 44: تست n8n Webhook
- [ ] در n8n یک workflow با Webhook node ایجاد کنید
- [ ] Webhook URL را از n8n کپی کنید
- [ ] در `/settings` → Integrations → n8n Webhook URL را وارد کنید
- [ ] از Cold Cake API به webhook درخواست بزنید
- [ ] بررسی کنید که n8n workflow اجرا می‌شود

**نتیجه مورد انتظار:** باید بتوانید از Cold Cake به n8n webhook ارسال کنید

#### قدم 45: تست Google Apps Script Integration
- [ ] مطمئن شوید Google Cloud Client ID و Secret تنظیم شده‌اند
- [ ] Google Apps Script را deploy کنید (از template موجود)
- [ ] بررسی کنید که می‌توانید فایل‌ها را از Google Drive بخوانید/بنویسید

**نتیجه مورد انتظار:** باید بتوانید با Google Drive کار کنید

#### قدم 46: تست Gemini API
- [ ] مطمئن شوید Gemini API Key تنظیم شده است
- [ ] در صفحه Content یک محتوا را انتخاب کنید
- [ ] روی دکمه AI Enhancement کلیک کنید
- [ ] بررسی کنید که Gemini API فراخوانی می‌شود

**نتیجه مورد انتظار:** باید بتوانید از Gemini API استفاده کنید

#### قدم 47: تست OpenAI API
- [ ] مطمئن شوید OpenAI API Key تنظیم شده است
- [ ] در صفحه Analytics روی Generate Predictions کلیک کنید
- [ ] بررسی کنید که OpenAI API برای تحلیل استفاده می‌شود

**نتیجه مورد انتظار:** باید بتوانید از OpenAI API استفاده کنید

#### قدم 48: تست End-to-End Workflow
- [ ] یک workflow کامل را تست کنید:
  1. مشتری جدید ایجاد کنید
  2. فروش ثبت کنید
  3. بدهی ثبت کنید
  4. پیام ارسال کنید
  5. گزارش export کنید
- [ ] بررسی کنید که همه چیز به درستی کار می‌کند

**نتیجه مورد انتظار:** باید workflow کامل به درستی کار کند

---

### بخش 7: تست Export (2 قدم)

#### قدم 49: تست Export کامل
- [ ] در صفحه Reports تمام Export ها را تست کنید:
  - [ ] Export Summary به Excel
  - [ ] Export Summary به PDF
  - [ ] Export Sales به Excel
  - [ ] Export Debts به Excel
  - [ ] Export Bank Ledger به Excel
  - [ ] Export Bank Ledger به PDF
- [ ] هر فایل را باز کنید و بررسی کنید که داده‌ها صحیح هستند
- [ ] بررسی کنید که فرمت تاریخ فارسی است
- [ ] بررسی کنید که اعداد فارسی هستند

**نتیجه مورد انتظار:** همه Export ها باید کار کنند و فرمت صحیح داشته باشند

#### قدم 50: تست Performance و Stability
- [ ] با داده‌های زیاد تست کنید:
  - [ ] 100+ مشتری
  - [ ] 200+ محصول
  - [ ] 500+ فروش
- [ ] بررسی کنید که صفحات به درستی لود می‌شوند
- [ ] بررسی کنید که جستجو سریع است
- [ ] بررسی کنید که Export با داده‌های زیاد کار می‌کند
- [ ] بررسی کنید که هیچ memory leak یا performance issue وجود ندارد

**نتیجه مورد انتظار:** سیستم باید با داده‌های زیاد هم به درستی کار کند

---

## Troubleshooting

### مشکل: "مشتری یافت نشد"
**علت:** هنوز هیچ مشتری در دیتابیس ایجاد نشده است  
**راه‌حل:** از API برای ایجاد مشتری استفاده کنید (قدم 9)

### مشکل: "خطا در اتصال به دیتابیس"
**علت:** Migration اجرا نشده یا DATABASE_URL اشتباه است  
**راه‌حل:** Migration را اجرا کنید و DATABASE_URL را بررسی کنید

### مشکل: "کد OTP دریافت نمی‌شود"
**علت:** Tabansms API key اشتباه است یا خط خدماتی فعال نیست  
**راه‌حل:** TABANSMS_API_KEY را بررسی کنید و با پشتیبانی تماس بگیرید

### مشکل: "Export Excel/PDF کار نمی‌کند"
**علت:** ممکن است مشکل در format داده‌ها باشد  
**راه‌حل:** لاگ‌های console را بررسی کنید و داده‌ها را validate کنید

### مشکل: "AI Enhancement کار نمی‌کند"
**علت:** API key تنظیم نشده یا quota تمام شده  
**راه‌حل:** API key ها را در Settings بررسی کنید

---

## نکات مهم

1. **همیشه ابتدا Migration اجرا کنید** - قبل از هر کاری
2. **داده‌های تست را از طریق API ایجاد کنید** - برای سرعت بیشتر
3. **API endpoints را ابتدا تست کنید** - قبل از تست UI
4. **Environment Variables را بررسی کنید** - قبل از استفاده از features
5. **لاگ‌ها را بررسی کنید** - برای debugging مشکلات

---

**آخرین بروزرسانی:** 28 دسامبر 2024  
**نسخه پروژه:** 0.1.0  
**آدرس Production:** https://coldcake.ir
